using UnityEngine;

namespace Tester
{
    public class sceneTester: MonoBehaviour
    {
        private void Start()
        {
            // foreach (var sect in GameManager.SectorMag.GetRandomSectorOptions())
            // {
            //     Debug.Log($"{GameManager.SectorMag.CurStage}, {GameManager.SectorMag.CurFloor}, {sect.sectorId}");
            // }
        }
    }
}